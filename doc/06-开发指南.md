# 智能股票交易辅助系统 - 开发指南

**版本**: v0.0.1-SNAPSHOT  
**最后更新**: 2026-03-18  
**文档状态**: 已按当前仓库结构对齐

---

## 1. 适用范围

本文档只针对当前仓库中的后端项目。

当前仓库：

- 有后端代码
- 有数据库初始化脚本
- 有测试代码
- 没有前端工程

---

## 2. 环境要求

| 软件 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.6+ |
| MySQL | 8.0+ |
| Redis | 7.0+ |

以下组件当前不是必需：

- Kafka
- MinIO
- Claude API
- 前端 Node 环境

---

## 3. 初始化步骤

### 3.1 克隆项目

```bash
git clone <repository-url>
cd smartstock-ai
```

### 3.2 初始化数据库

```bash
mysql -u root -p -e "CREATE DATABASE smartstock_ai DEFAULT CHARACTER SET utf8mb4;"
mysql -u root -p smartstock_ai < doc/sql/init.sql
```

### 3.3 启动 Redis

```bash
redis-server
```

如果你本地 Redis 需要密码，请确认和配置文件一致。

---

## 4. 配置方式

当前配置文件：`src/main/resources/application.yml`

默认支持环境变量覆盖。

常用变量：

```bash
export SMARTSTOCK_DB_URL='jdbc:mysql://127.0.0.1:3306/smartstock_ai?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai'
export SMARTSTOCK_DB_USERNAME='root'
export SMARTSTOCK_DB_PASSWORD='your-password'
export SMARTSTOCK_REDIS_HOST='127.0.0.1'
export SMARTSTOCK_REDIS_PORT='6379'
export SMARTSTOCK_REDIS_PASSWORD='your-redis-password'
export SMARTSTOCK_JWT_SECRET='your-32-byte-secret'
export SMARTSTOCK_SERVER_PORT='8080'
```

注意：

- `SMARTSTOCK_JWT_SECRET` 建议至少 32 字节
- 当前没有 `dev/test/prod` 多 profile 体系

---

## 5. 启动项目

### 5.1 运行测试

```bash
mvn test
```

### 5.2 启动服务

```bash
mvn spring-boot:run
```

启动后默认访问：

```text
http://127.0.0.1:8080
```

---

## 6. 当前目录结构

```text
smartstock-ai/
├── doc/
├── src/main/java/com/smartstock/
│   ├── client/
│   ├── common/
│   ├── config/
│   ├── controller/
│   ├── dto/
│   ├── entity/
│   ├── mapper/
│   ├── service/
│   ├── util/
│   └── vo/
├── src/main/resources/
├── src/test/java/com/smartstock/
└── pom.xml
```

---

## 7. 当前开发重点

当前代码主要集中在以下模块：

- 用户与 JWT
- 行情
- 自选股
- AI 分析与问答

当前尚未进入开发的模块：

- 回测
- 风险预警
- 微服务拆分

---

## 8. 测试说明

当前测试覆盖主要包括：

- `JwtUtilTest`
- `JwtAuthInterceptorTest`
- `GlobalExceptionHandlerTest`
- `ApiValidationTest`
- `QueryParamValidationTest`
- `MarketServiceImplTest`
- `EastMoneyClientTest`

当前测试总数：`28`

### 推荐测试命令

跑全量测试：

```bash
mvn test
```

只跑股票相关测试：

```bash
mvn test -Dtest=EastMoneyClientTest,MarketServiceImplTest,QueryParamValidationTest,ApiValidationTest
```

---

## 9. 联调说明

### 9.1 登录流程

1. 调 `POST /api/auth/register`
2. 调 `POST /api/auth/login`
3. 取返回的 `token`
4. 访问受保护接口时加上：

```text
Authorization: Bearer <token>
```

### 9.2 行情联调

当前行情来自东方财富 HTTP 接口，联调时请注意：

- 外部接口偶尔可能慢或波动
- 项目有 Redis 缓存
- K 线接口当前会按 `limit` 截断返回

---

## 10. 当前已知边界

以下不是 bug，而是当前范围限制：

- 没有前端工程
- 没有 WebSocket
- 没有回测
- 没有风险预警

---

## 11. 文档使用建议

如果你要继续开发，建议先看：

1. `README.md`
2. `doc/03-技术架构文档.md`
3. `doc/04-数据库设计.md`
4. `doc/05-API接口文档.md`

/**
 * Design rules for recreating coded UIs in Figma.
 * The core idea: read the CSS, match it exactly in Figma, one element at a time.
 */
export declare const DESIGN_RULES: Record<string, string>;

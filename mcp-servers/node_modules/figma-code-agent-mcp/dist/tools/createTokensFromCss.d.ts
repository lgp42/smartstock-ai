/**
 * MCP tool: create_tokens_from_css
 * Parses CSS custom properties and creates Figma Variables via batch createToken calls.
 * Optionally creates a visual swatch board for color tokens.
 */
export declare const createTokensFromCssSchema: {
    name: string;
    description: string;
    inputSchema: {
        type: "object";
        properties: {
            css: {
                type: string;
                description: string;
            };
            collection: {
                type: string;
                description: string;
            };
            createSwatches: {
                type: string;
                description: string;
            };
            prefix: {
                type: string;
                description: string;
            };
        };
        required: string[];
    };
};
/**
 * Execute the CSS → Figma Variables tool.
 */
export declare function createTokensFromCss(args: Record<string, unknown>): Promise<unknown>;

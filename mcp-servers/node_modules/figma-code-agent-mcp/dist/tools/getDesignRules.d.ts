/**
 * Schema for the get_design_rules tool — returns rules for reproducing
 * coded UIs in Figma by reading CSS and matching styles exactly.
 */
export declare const getDesignRulesSchema: {
    name: string;
    description: string;
    inputSchema: {
        type: "object";
        properties: {
            topic: {
                type: string;
                description: string;
                enum: string[];
                default: string;
            };
        };
    };
};
